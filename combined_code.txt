--- Файл: src\app\globals.css ---

@import "tailwindcss";

@layer base {
  :root {
    --font-sans: var(--font-geist-sans), system-ui, sans-serif;
    --font-mono: var(--font-geist-mono), monospace;
  }
  
  body {
    background-color: #09090b; /* Zinc 950 */
    color: #f4f4f5; /* Zinc 100 */
    font-family: var(--font-sans);
    overflow-x: hidden;
  }
}

/* Кастомные визуальные утилиты для параметров 10/10/10 */
.grid-mesh {
  background-image: 
    linear-gradient(to right, rgba(255, 255, 255, 0.03) 1px, transparent 1px),
    linear-gradient(to bottom, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
  background-size: 24px 24px;
}

.liquid-glass {
  background: rgba(9, 9, 11, 0.8);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.07);
  box-shadow: 
    inset 0 1px 0 rgba(255, 255, 255, 0.05),
    0 10px 30px -10px rgba(0, 0, 0, 0.7);
}

.glow-rose {
  box-shadow: 0 0 20px -5px rgba(244, 63, 94, 0.2);
}

.glow-emerald {
  box-shadow: 0 0 20px -5px rgba(16, 185, 129, 0.2);
}

/* Компактный скроллбар для плотного расположения информации */
::-webkit-scrollbar {
  width: 4px;
  height: 4px;
}
::-webkit-scrollbar-track {
  background: #09090b;
}
::-webkit-scrollbar-thumb {
  background: #27272a;
  border-radius: 2px;
}
::-webkit-scrollbar-thumb:hover {
  background: #3f3f46;
}

--- Файл: src\app\layout.tsx ---

import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin", "cyrillic"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Народный Аудитор Кейсов | Независимый математический мониторинг",
  description: "Аналитический краудсорсинговый портал и расширение для разоблачения скрытых алгоритмов сайтов с кейсами.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ru" className="dark scroll-smooth">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased bg-zinc-950 text-zinc-100 selection:bg-rose-500/30 selection:text-rose-200`}>
        {children}
      </body>
    </html>
  );
}

--- Файл: src\app\page.tsx ---

"use client";

import React from "react";
import StreamFeed from "@/app/components/StreamFeed";
import AuditCharts from "@/app/components/AuditCharts";
import StrategySandbox from "@/app/components/StrategySandbox";
import ExtensionBanner from "@/app/components/ExtensionBanner";
import { Shield, Eye, Warning, Database, FileCode, UsersFour } from "@phosphor-icons/react";

export default function Home() {
  return (
    <main className="min-h-dvh relative overflow-hidden bg-zinc-950 grid-mesh px-4 py-6 md:p-8 flex flex-col gap-6">
      
      {/* Фоновые градиенты */}
      {/* Основная асимметричная сетка */}
      <div className="w-full max-w-350 mx-auto grid grid-cols-1 lg:grid-cols-12 gap-4 flex-1 z-10 items-start"></div>
      <div className="absolute top-1/4 left-1/4 w-100 h-100 rounded-full bg-rose-950/10 blur-[100px] pointer-events-none" />
      <div className="absolute bottom-1/4 right-1/4 w-87.5 h-87.5 rounded-full bg-zinc-900/40 blur-[80px] pointer-events-none" />

      {/* Верхний статус-бар */}
      <header className="w-full max-w-350 mx-auto flex flex-col md:flex-row md:items-center justify-between gap-4 border-b border-zinc-800 pb-4 z-10">
        <div className="flex flex-col">
          <div className="flex items-center gap-2">
            <div className="w-2.5 h-2.5 rounded bg-rose-500 animate-pulse" />
            <h1 className="text-base font-bold font-mono tracking-tighter uppercase text-white">
              Народный Аудитор Кейсов
            </h1>
          </div>
          <span className="text-[10px] font-mono text-zinc-500 tracking-wider">
            НЕЗАВИСИМЫЙ МАТЕМАТИЧЕСКИЙ МОНИТОРИНГ / КРАУДСОРСИНГ
          </span>
        </div>

        {/* Сводная глобальная телеметрия */}
        <div className="flex flex-wrap gap-4 text-[10px] font-mono">
          <div className="flex flex-col border-l border-zinc-800 pl-3">
            <span className="text-zinc-500 uppercase">Проверено сайтов</span>
            <span className="text-sm font-bold text-zinc-100">14 площадок</span>
          </div>
          <div className="flex flex-col border-l border-zinc-800 pl-3">
            <span className="text-zinc-500 uppercase">Логированных открытий</span>
            <span className="text-sm font-bold text-zinc-100">1,482,049</span>
          </div>
          <div className="flex flex-col border-l border-zinc-800 pl-3">
            <span className="text-zinc-500 uppercase">Реальный средний RTP</span>
            <span className="text-sm font-bold text-rose-500">22.4%</span>
          </div>
        </div>
      </header>

      {/* Основная асимметричная сетка */}
      <div className="w-full max-w-350 mx-auto grid grid-cols-1 lg:grid-cols-12 gap-4 flex-1 z-10">
        
        {/* ЛЕВАЯ КОЛОНКА (4/12): Концепт, Проблема и баннер расширения */}
        <div className="lg:col-span-4 flex flex-col gap-4">
          
          <div className="bg-zinc-950/60 border border-zinc-800 p-5 rounded-3xl liquid-glass flex flex-col gap-3">
            <div className="flex items-center gap-1.5 text-[9px] font-mono text-zinc-500">
              <Shield className="w-4 h-4 text-rose-500" />
              <span>СТАТУС: ДЕЦЕНТРАЛИЗОВАННЫЙ АУДИТ АЛГОРИТМОВ</span>
            </div>
            
            <h2 className="text-2xl font-black text-zinc-100 tracking-tight leading-none uppercase">
              Разоблачаем скрытую <span className="text-rose-500">подкрутку</span>
            </h2>

            <p className="text-zinc-400 text-xs leading-relaxed mt-1">
              Сторонние сайты по открытию кейсов скрывают реальные шансы на выигрыш. Мы собираем математически точные, сухие данные об открытиях реальных игроков и формируем единую независимую базу окупаемости.
            </p>

            <div className="flex flex-col gap-2 border-t border-zinc-800 pt-3 mt-2">
              <div className="flex items-start gap-2 text-[11px]">
                <Warning className="w-4 h-4 text-rose-500 shrink-0 mt-0.5" />
                <p className="text-zinc-300">
                  <strong className="text-zinc-100 font-bold">Скрытые скрипты:</strong> Владельцы сайтов динамически урезают шансы на выигрыш во время наплыва аудитории.
                </p>
              </div>
              <div className="flex items-start gap-2 text-[11px]">
                <Eye className="w-4 h-4 text-rose-500 shrink-0 mt-0.5" />
                <p className="text-zinc-300">
                  <strong className="text-zinc-100 font-bold">Рекламный обман:</strong> Блогеры рекламируют подкрученные индивидуальные аккаунты с завышенной окупаемостью.
                </p>
              </div>
            </div>
          </div>

          <div className="flex-1">
            <ExtensionBanner />
          </div>

        </div>

        {/* ЦЕНТРАЛЬНАЯ КОЛОНКА (5/12): Графики и симулятор */}
        <div className="lg:col-span-5 flex flex-col gap-4">
          
          <div className="flex-1">
            <AuditCharts />
          </div>

          <div className="flex-1">
            <StrategySandbox />
          </div>

        </div>

        {/* ПРАВАЯ КОЛОНКА (3/12): Живой поток логов */}
        <div className="lg:col-span-3 flex flex-col h-full min-h-100">
          <StreamFeed />
        </div>

      </div>

      {/* Системный футер */}
      <footer className="w-full max-w-350 mx-auto border-t border-zinc-800 pt-4 flex flex-col md:flex-row md:items-center justify-between gap-3 text-[10px] font-mono text-zinc-500 z-10">
        <div className="flex items-center gap-3">
          <div className="flex items-center gap-1">
            <Database className="w-3.5 h-3.5 text-rose-500 animate-pulse" />
            <span>DB_STATUS: SYNCED</span>
          </div>
          <div className="flex items-center gap-1">
            <UsersFour className="w-3.5 h-3.5 text-zinc-400" />
            <span>NODE_COUNT: 2,492 NODES</span>
          </div>
          <div className="flex items-center gap-1">
            <FileCode className="w-3.5 h-3.5 text-zinc-400" />
            <span>CONTRACT: VERIFIED</span>
          </div>
        </div>
        <div>
          <span>CASEAUDIT v1.1.0-BETA © 2026. Исходный код открыт и верифицирован на соответствие GDPR.</span>
        </div>
      </footer>
    </main>
  );
}

--- Файл: src\app\components\AuditCharts.tsx ---

"use client";

import React, { useState } from "react";
import { motion } from "framer-motion";
import { 
  AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, 
  BarChart, Bar, Cell, ReferenceLine
} from "recharts";
import { TrendUp, CalendarCheck, Clock, Warning } from "@phosphor-icons/react";

const AUDITED_CASES = [
  { id: "cs2-knife", name: "CS2 Случайный Нож", site: "drop-skin.net", url: "https://drop-skin.net/cases/knife" },
  { id: "dota-arcana", name: "Dota 2 Arcana Case", site: "easycase.pro", url: "https://easycase.pro/box/arcana" },
  { id: "rust-neon", name: "Rust Neon Weapon Box", site: "loot-box.org", url: "https://loot-box.org/crates/neon" },
];

const weeklyData = [
  { day: "18.05", rtp: 32 },
  { day: "19.05", rtp: 31 },
  { day: "20.05", rtp: 28 },
  { day: "21.05", rtp: 34 },
  { day: "22.05", rtp: 16 },
  { day: "23.05", rtp: 12 },
  { day: "24.05", rtp: 15 },
  { day: "25.05", rtp: 29 },
];

const dayOfWeekData = [
  { name: "ПН", rtp: 38, info: "Стандарт" },
  { name: "ВТ", rtp: 41, info: "Макс. отдача" },
  { name: "СР", rtp: 36, info: "Стандарт" },
  { name: "ЧТ", rtp: 33, info: "Плавное падение" },
  { name: "ПТ", rtp: 15, info: "Затягивание гаек" },
  { name: "СБ", rtp: 11, info: "Критически низкий" },
  { name: "ВС", rtp: 18, info: "Затягивание гаек" },
];

const hourlyData = [
  { hour: "00:00", rtp: 21 },
  { hour: "02:00", rtp: 26 },
  { hour: "04:00", rtp: 35 },
  { hour: "06:00", rtp: 42 },
  { hour: "08:00", rtp: 39 },
  { hour: "10:00", rtp: 25 },
  { hour: "12:00", rtp: 18 },
  { hour: "14:00", rtp: 15 },
  { hour: "16:00", rtp: 14 },
  { hour: "18:00", rtp: 11 },
  { hour: "20:00", rtp: 9 },
  { hour: "22:00", rtp: 14 },
];

interface TabConfig {
  id: "weekly" | "dayOfWeek" | "hourly";
  label: string;
  icon: React.ComponentType<{ className?: string }>;
}

const tabs: TabConfig[] = [
  { id: "weekly", label: "Неделя", icon: TrendUp },
  { id: "dayOfWeek", label: "По дням недели (Анализ Гаек)", icon: CalendarCheck },
  { id: "hourly", label: "Суточный (По часам)", icon: Clock },
];

export default function AuditCharts() {
  const [activeTab, setActiveTab] = useState<"weekly" | "dayOfWeek" | "hourly">("dayOfWeek");
  const [selectedCase, setSelectedCase] = useState(AUDITED_CASES[0]);

  return (
    <div className="bg-zinc-950/40 border border-zinc-800 p-4 rounded-3xl liquid-glass flex flex-col gap-4 h-full">
      <div className="flex flex-col md:flex-row md:items-center justify-between gap-3 border-b border-zinc-800 pb-3">
        <div>
          <span className="text-[10px] font-mono uppercase tracking-widest text-zinc-500">ВЫБОР ЦЕЛЕГО КЕЙСА</span>
          <div className="flex flex-wrap gap-1.5 mt-1.5">
            {AUDITED_CASES.map((item) => (
              <button
                key={item.id}
                onClick={() => setSelectedCase(item)}
                className={`px-2.5 py-1 text-xs font-mono border rounded-lg transition-all duration-200 cursor-pointer ${
                  selectedCase.id === item.id
                    ? "bg-rose-500/10 border-rose-500 text-rose-300"
                    : "bg-zinc-900/50 border-zinc-800 text-zinc-400 hover:border-zinc-700"
                }`}
              >
                {item.name}
              </button>
            ))}
          </div>
        </div>

        <div className="flex flex-col items-end">
          <span className="text-[9px] font-mono text-zinc-500">ГЛУБОКАЯ СВЯЗЬ РАСШИРЕНИЯ</span>
          <a
            href={selectedCase.url}
            target="_blank"
            rel="noopener noreferrer"
            className="mt-1 flex items-center gap-1.5 px-2.5 py-1 text-[11px] font-mono bg-zinc-900 border border-zinc-800 text-zinc-300 rounded-lg hover:bg-zinc-800 hover:text-white hover:border-zinc-600 transition-all active:scale-[0.98]"
          >
            <span>Посетить сайт кейса</span>
            <span className="text-[9px] px-1 py-0.2 rounded bg-rose-500/20 text-rose-400 border border-rose-500/30">
              AUDIT ACTIVE
            </span>
          </a>
        </div>
      </div>

      <div className="flex items-center justify-between bg-zinc-900/50 border border-zinc-800 p-1 rounded-xl">
        <div className="flex w-full gap-1">
          {tabs.map((tab) => {
            const Icon = tab.icon;
            const isActive = activeTab === tab.id;
            return (
              <button
                key={tab.id}
                onClick={() => setActiveTab(tab.id)}
                className="relative flex-1 py-1.5 px-2 rounded-lg text-xs font-mono flex items-center justify-center gap-1.5 cursor-pointer z-10 transition-colors"
                style={{ color: isActive ? "#ffffff" : "#a1a1aa" }}
              >
                {isActive && (
                  <motion.div
                    layoutId="active-chart-tab"
                    className="absolute inset-0 bg-zinc-800 border border-zinc-700 rounded-lg -z-10"
                    transition={{ type: "spring", stiffness: 180, damping: 18 }}
                  />
                )}
                <Icon className="w-4 h-4" />
                <span className="truncate">{tab.label}</span>
              </button>
            );
          })}
        </div>
      </div>

      <div className="flex-1 min-h-55 relative">
        <ResponsiveContainer width="100%" height="100%">
          {activeTab === "weekly" ? (
            <AreaChart data={weeklyData} margin={{ top: 10, right: 10, left: -25, bottom: 0 }}>
              <defs>
                <linearGradient id="colorRtp" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="5%" stopColor="#f43f5e" stopOpacity={0.3}/>
                  <stop offset="95%" stopColor="#f43f5e" stopOpacity={0}/>
                </linearGradient>
              </defs>
              <XAxis dataKey="day" stroke="#52525b" fontSize={9} fontFamily="var(--font-geist-mono)" tickLine={false} />
              <YAxis stroke="#52525b" fontSize={9} fontFamily="var(--font-geist-mono)" tickLine={false} unit="%" />
              <Tooltip
                contentStyle={{ background: "#09090b", border: "1px solid #3f3f46", fontSize: "11px", fontFamily: "var(--font-geist-mono)" }}
                formatter={(value: unknown) => {
                const valStr = value !== undefined && value !== null ? String(value) : "0";
                return [`${valStr}%`, "RTP отдачи"];
                }}
              />
              <Area type="monotone" dataKey="rtp" stroke="#f43f5e" strokeWidth={1.5} fillOpacity={1} fill="url(#colorRtp)" />
              <ReferenceLine y={45} stroke="#10b981" strokeDasharray="3 3" label={{ value: "Честный RTP (45%)", fill: "#10b981", fontSize: 9, position: "top" }} />
            </AreaChart>
          ) : activeTab === "dayOfWeek" ? (
            <BarChart data={dayOfWeekData} margin={{ top: 10, right: 10, left: -25, bottom: 0 }}>
              <XAxis dataKey="name" stroke="#52525b" fontSize={9} fontFamily="var(--font-geist-mono)" tickLine={false} />
              <YAxis stroke="#52525b" fontSize={9} fontFamily="var(--font-geist-mono)" tickLine={false} unit="%" />
              <Tooltip
                contentStyle={{ background: "#09090b", border: "1px solid #3f3f46", fontSize: "11px", fontFamily: "var(--font-geist-mono)" }}
                formatter={(value: unknown, _name: unknown, props: unknown) => {
                    const valStr = value !== undefined && value !== null ? String(value) : "0";
                    const payloadEntry = props as { payload?: { info?: string } } | undefined;
                    const info = payloadEntry?.payload?.info || "";
                    return [`${valStr}%`, info];
                    }}
              />
              <Bar dataKey="rtp" radius={[4, 4, 0, 0]}>
                {dayOfWeekData.map((entry, index) => {
                  const isRigged = entry.name === "ПТ" || entry.name === "СБ" || entry.name === "ВС";
                  return (
                    <Cell 
                      key={`cell-${index}`} 
                      fill={isRigged ? "#f43f5e" : "#52525b"} 
                    />
                  );
                })}
              </Bar>
              <ReferenceLine y={45} stroke="#10b981" strokeDasharray="3 3" />
            </BarChart>
          ) : (
            <AreaChart data={hourlyData} margin={{ top: 10, right: 10, left: -25, bottom: 0 }}>
              <defs>
                <linearGradient id="colorHourly" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="5%" stopColor="#ef4444" stopOpacity={0.25}/>
                  <stop offset="95%" stopColor="#ef4444" stopOpacity={0}/>
                </linearGradient>
              </defs>
              <XAxis dataKey="hour" stroke="#52525b" fontSize={9} fontFamily="var(--font-geist-mono)" tickLine={false} />
              <YAxis stroke="#52525b" fontSize={9} fontFamily="var(--font-geist-mono)" tickLine={false} unit="%" />
              <Tooltip
                contentStyle={{ background: "#09090b", border: "1px solid #3f3f46", fontSize: "11px", fontFamily: "var(--font-geist-mono)" }}
                formatter={(value: unknown) => {
                const valStr = value !== undefined && value !== null ? String(value) : "0";
                return [`${valStr}%`, "Текущая отдача"];
                }}
              />
              <Area type="step" dataKey="rtp" stroke="#ef4444" strokeWidth={1} fillOpacity={1} fill="url(#colorHourly)" />
            </AreaChart>
          )}
        </ResponsiveContainer>
      </div>

      <div className="bg-rose-950/20 border border-rose-900/40 p-2.5 rounded-xl flex items-start gap-2">
        <Warning className="w-5 h-5 text-rose-500 shrink-0 mt-0.5" />
        <div className="text-[11px] leading-relaxed">
          {activeTab === "dayOfWeek" ? (
            <p className="text-rose-200">
              <strong className="text-white">Внимание (Затягивание гаек):</strong> Историческая агрегация за 60 дней показывает падение отдачи до <span className="font-mono font-semibold text-rose-400 text-xs">11-15%</span> по пятницам и субботам. Владельцы сайтов систематически занижают шансы на выходных, когда приток геймеров максимален.
            </p>
          ) : activeTab === "hourly" ? (
            <p className="text-rose-200">
              <strong className="text-white">Манипуляция часовым пулом:</strong> В ночные часы <span className="font-mono text-emerald-400 font-semibold">(04:00 - 08:00)</span> отдача зафиксирована выше нормы для создания иллюзии легкого выигрыша среди малочисленной аудитории. К вечеру шансы резко снижаются.
            </p>
          ) : (
            <p className="text-rose-200">
              <strong className="text-white">Недельный тренд:</strong> Средняя рентабельность открытия кейса <span className="font-mono font-bold text-rose-400">«{selectedCase.name}»</span> ни разу за неделю не превысила критический порог в <span className="font-mono font-bold">34%</span>. Математическое ожидание глубоко убыточное.
            </p>
          )}
        </div>
      </div>
    </div>
  );
}

--- Файл: src\app\components\ExtensionBanner.tsx ---

"use client";

import React, { useState, useEffect } from "react";
import { motion } from "framer-motion";
import { DownloadSimple, Cpu, Code, Users } from "@phosphor-icons/react";

export default function ExtensionBanner() {
  const [downloadCount, setDownloadCount] = useState(24382);

  useEffect(() => {
    const interval = setInterval(() => {
      setDownloadCount(prev => prev + Math.floor(Math.random() * 3) + 1);
    }, 7000);
    return () => clearInterval(interval);
  }, []);

  return (
    <div className="relative bg-zinc-950/60 border border-rose-500/30 p-5 rounded-3xl liquid-glass overflow-hidden flex flex-col justify-between h-full glow-rose">
      <div className="absolute inset-0 grid-mesh opacity-10 pointer-events-none" />
      <div className="absolute -right-20 -top-20 w-44 h-44 rounded-full bg-rose-500/10 blur-[60px] pointer-events-none" />

      <div className="relative z-10 flex flex-col gap-3">
        <div className="flex items-center gap-2">
          <Cpu className="w-5 h-5 text-rose-500 animate-pulse" />
          <span className="text-[10px] font-mono uppercase tracking-widest text-rose-400">КРАУДСОРСИНГОВЫЙ МОДУЛЬ</span>
        </div>

        <h3 className="text-xl font-bold text-zinc-100 tracking-tight leading-snug">
          Анонимный аудит из вашего браузера
        </h3>

        <p className="text-zinc-400 text-xs leading-relaxed max-w-70">
          Установите легковесное расширение. Оно работает абсолютно незаметно в фоновом режиме, фиксируя реальный математический результат выпадения скинов без доступа к вашим персональным данным или аккаунту Steam.
        </p>

        <div className="flex flex-wrap gap-1 mt-1 text-[9px] font-mono text-zinc-500">
          <span className="px-1.5 py-0.5 rounded bg-zinc-900 border border-zinc-800">CHROME</span>
          <span className="px-1.5 py-0.5 rounded bg-zinc-900 border border-zinc-800">FIREFOX</span>
          <span className="px-1.5 py-0.5 rounded bg-zinc-900 border border-zinc-800">OPERA</span>
          <span className="px-1.5 py-0.5 rounded bg-zinc-900 border border-zinc-800">EDGE</span>
        </div>
      </div>

      <div className="relative z-10 mt-6 flex flex-col gap-3">
        <motion.a
          href="https://github.com"
          target="_blank"
          rel="noopener noreferrer"
          whileHover={{ scale: 1.02, y: -2 }}
          whileTap={{ scale: 0.98 }}
          transition={{ type: "spring", stiffness: 200, damping: 12 }}
          className="w-full py-2.5 bg-rose-500 hover:bg-rose-600 text-white font-mono font-bold text-xs rounded-2xl flex items-center justify-center gap-2 transition-all shadow-md shadow-rose-950/40 cursor-pointer"
        >
          <DownloadSimple className="w-4 h-4 animate-bounce" />
          <span>СКАЧАТЬ РАСШИРЕНИЕ</span>
        </motion.a>

        <div className="flex items-center justify-between text-[10px] font-mono text-zinc-500 border-t border-zinc-800 pt-3 mt-1">
          <div className="flex items-center gap-1">
            <Users className="w-3.5 h-3.5 text-zinc-400" />
            <span>Аудиторов в сети:</span>
          </div>
          <span className="font-bold text-zinc-300">
            {downloadCount.toLocaleString("ru-RU")}
          </span>
        </div>

        <div className="flex items-center justify-between text-[10px] font-mono text-zinc-500">
          <div className="flex items-center gap-1">
            <Code className="w-3.5 h-3.5 text-zinc-400" />
            <span>Исходный код:</span>
          </div>
          <a href="#github" className="text-zinc-400 hover:text-white underline">
            GitHub Repo
          </a>
        </div>
      </div>
    </div>
  );
}

--- Файл: src\app\components\StrategySandbox.tsx ---

"use client";

import React, { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { Plus, Trash, Play, ShieldCheck, Info, Sparkle } from "@phosphor-icons/react";

interface Step {
  id: string;
  type: "open" | "upgrade" | "contract";
  param: string;
  cost: number;
}

export default function StrategySandbox() {
  const [steps, setSteps] = useState<Step[]>([
    { id: "s1", type: "open", param: "Кейс Нож (x1)", cost: 450 },
    { id: "s2", type: "upgrade", param: "Контракт 40% шанс", cost: 150 },
  ]);
  const [isSimulating, setIsSimulating] = useState(false);
  const [simulationResult, setSimulationResult] = useState<{
    totalCost: number;
    simulatedRtp: number;
    ruinProbability: number;
    isCalculated: boolean;
  } | null>(null);

  const addStep = () => {
    const newStep: Step = {
      id: `step-${Date.now()}`,
      type: "open",
      param: "Кейс Оружие (x1)",
      cost: 150,
    };
    setSteps([...steps, newStep]);
  };

  const deleteStep = (id: string) => {
    setSteps(steps.filter(step => step.id !== id));
  };

  const updateStep = (id: string, updatedFields: Partial<Step>) => {
    setSteps(steps.map(step => (step.id === id ? { ...step, ...updatedFields } : step)));
  };

  const runSimulation = () => {
    setIsSimulating(true);
    setSimulationResult(null);
    
    setTimeout(() => {
      const sumCost = steps.reduce((acc, curr) => acc + curr.cost, 0);
      const baseRtp = 18 + Math.random() * 8; 
      const ruinRate = 72 + Math.random() * 15;

      setSimulationResult({
        totalCost: sumCost,
        simulatedRtp: Math.round(baseRtp * 10) / 10,
        ruinProbability: Math.round(ruinRate * 10) / 10,
        isCalculated: true,
      });
      setIsSimulating(false);
    }, 2000);
  };

  return (
    <div className="bg-zinc-950/40 border border-zinc-800 p-4 rounded-3xl liquid-glass flex flex-col gap-4 h-full">
      <div className="border-b border-zinc-800 pb-3 flex justify-between items-center">
        <div>
          <span className="text-[10px] font-mono uppercase tracking-widest text-zinc-500">СИМУЛЯТОР СТРАТЕГИЙ (PRO)</span>
          <h3 className="text-sm font-bold text-zinc-100 mt-0.5">Песочница цепочек поведения</h3>
        </div>
        <span className="text-[9px] px-2 py-0.5 rounded bg-amber-500/10 text-amber-400 border border-amber-500/30 font-mono uppercase">
          PRO-MOD
        </span>
      </div>

      <p className="text-zinc-400 text-[11px] leading-relaxed">
        Спроектируйте последовательность действий на кейс-сайте. Система смоделирует ее прохождение на основе исторического массива логов из расширения.
      </p>

      <div className="flex-1 flex flex-col gap-2 max-h-70 overflow-y-auto pr-1">
        <AnimatePresence initial={false}>
          {steps.map((step, index) => (
            <motion.div
              key={step.id}
              initial={{ opacity: 0, x: -30 }}
              animate={{ opacity: 1, x: 0 }}
              exit={{ opacity: 0, scale: 0.9, x: 30 }}
              layout
              transition={{ type: "spring", stiffness: 150, damping: 15 }}
              className="flex items-center gap-2 bg-zinc-900/60 border border-zinc-800 p-2 rounded-xl"
            >
              <span className="text-[10px] font-mono text-zinc-600 font-bold w-5">
                {String(index + 1).padStart(2, "0")}
              </span>

              <select
                value={step.type}
                onChange={(e) => updateStep(step.id, { type: e.target.value as "open" | "upgrade" | "contract" })}
                className="bg-zinc-950 border border-zinc-800 text-[11px] font-mono p-1 rounded text-zinc-300 focus:outline-none focus:border-rose-500"
              >
                <option value="open">КЕЙС</option>
                <option value="upgrade">АПГРЕЙД</option>
                <option value="contract">КОНТРАКТ</option>
              </select>

              <input
                type="text"
                value={step.param}
                onChange={(e) => updateStep(step.id, { param: e.target.value })}
                className="flex-1 min-w-15 bg-zinc-950 border border-zinc-800 text-[11px] font-mono p-1 rounded text-zinc-200 focus:outline-none focus:border-rose-500"
                placeholder="Объект"
              />

              <div className="flex items-center gap-1">
                <input
                  type="number"
                  value={step.cost || ""}
                  onChange={(e) => updateStep(step.id, { cost: Number(e.target.value) })}
                  className="w-14 bg-zinc-950 border border-zinc-800 text-[11px] font-mono p-1 rounded text-rose-400 text-right focus:outline-none focus:border-rose-500"
                  placeholder="Стоимость"
                />
                <span className="text-[9px] font-mono text-zinc-600">₽</span>
              </div>

              <button
                onClick={() => deleteStep(step.id)}
                className="text-zinc-500 hover:text-rose-500 transition-colors cursor-pointer p-1"
              >
                <Trash className="w-3.5 h-3.5" />
              </button>
            </motion.div>
          ))}
        </AnimatePresence>

        <button
          onClick={addStep}
          className="py-1.5 border border-dashed border-zinc-800 hover:border-zinc-500 hover:bg-zinc-900/40 text-zinc-500 hover:text-zinc-300 transition-all text-[11px] font-mono rounded-xl flex items-center justify-center gap-1 cursor-pointer"
        >
          <Plus className="w-3.5 h-3.5" />
          <span>Добавить шаг в стратегию</span>
        </button>
      </div>

      <div className="border-t border-zinc-800 pt-3 flex flex-col gap-3">
        <button
          onClick={runSimulation}
          disabled={isSimulating || steps.length === 0}
          className="w-full py-2 bg-rose-500 text-white hover:bg-rose-600 font-mono text-xs font-bold rounded-xl flex items-center justify-center gap-1.5 transition-all shadow-lg shadow-rose-950/30 cursor-pointer active:scale-[0.98] disabled:opacity-50 disabled:cursor-not-allowed"
        >
          {isSimulating ? (
            <div className="w-3.5 h-3.5 border-2 border-white/30 border-t-white rounded-full animate-spin" />
          ) : (
            <Play className="w-3.5 h-3.5 fill-current" />
          )}
          <span>{isSimulating ? "Расчет по логам базы данных..." : "Запустить симуляцию на БД"}</span>
        </button>

        <AnimatePresence>
          {simulationResult && (
            <motion.div
              initial={{ opacity: 0, y: 15, scale: 0.98 }}
              animate={{ opacity: 1, y: 0, scale: 1 }}
              exit={{ opacity: 0, y: 15 }}
              className="bg-zinc-900/80 border border-zinc-800 p-3 rounded-xl flex flex-col gap-1.5"
            >
              <div className="flex items-center gap-1 text-[10px] font-mono text-amber-400">
                <Sparkle className="w-3.5 h-3.5 animate-pulse" />
                <span>РЕЗУЛЬТАТ МАТЕМАТИЧЕСКОГО МОДЕЛИРОВАНИЯ</span>
              </div>

              <div className="grid grid-cols-3 gap-2 border-t border-b border-zinc-800 py-2 mt-1">
                <div className="flex flex-col">
                  <span className="text-[8px] font-mono text-zinc-500 uppercase">Бюджет</span>
                  <span className="text-xs font-mono font-bold text-zinc-200">{simulationResult.totalCost} ₽</span>
                </div>
                <div className="flex flex-col">
                  <span className="text-[8px] font-mono text-zinc-500 uppercase">Ожидаемый RTP</span>
                  <span className="text-xs font-mono font-bold text-rose-500">{simulationResult.simulatedRtp}%</span>
                </div>
                <div className="flex flex-col">
                  <span className="text-[8px] font-mono text-zinc-500 uppercase">Слив в 0%</span>
                  <span className="text-xs font-mono font-bold text-rose-400">{simulationResult.ruinProbability}%</span>
                </div>
              </div>

              <div className="flex items-start gap-1.5 text-[9px] text-zinc-400 mt-1 leading-relaxed">
                <Info className="w-4 h-4 text-zinc-500 shrink-0 mt-0.5" />
                <span>Данная последовательность действий с вероятностью <strong>{simulationResult.ruinProbability}%</strong> ведет к полной потере средств на шаге контракта из-за скрытых урезаний множителей.</span>
              </div>
            </motion.div>
          )}
        </AnimatePresence>

        <div className="flex items-center gap-1.5 justify-center text-[9px] font-mono text-zinc-600">
          <ShieldCheck className="w-3.5 h-3.5 text-emerald-600" />
          <span>Просчитано на основе 147,820 реальных аудиторских логов</span>
        </div>
      </div>
    </div>
  );
}

--- Файл: src\app\components\StreamFeed.tsx ---

"use client";

import React, { useEffect, useState, useRef } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { Terminal, Shield, ArrowUpRight, ArrowDownRight } from "@phosphor-icons/react";

interface AuditLog {
  id: string;
  timestamp: string;
  user: string;
  site: string;
  caseName: string;
  cost: number;
  dropName: string;
  marketValue: number;
  isProfit: boolean;
}

const SIMULATED_SITES = ["drop-skin.net", "easycase.pro", "loot-box.org", "cs-win.co"];
const SIMULATED_USERS = ["xX_demon_cs2", "mirage_king", "sk1n_hunter", "flame_clutch", "b0b_trade", "pasha_fan", "drop_or_rip"];
const SIMULATED_CASES = [
  { name: "Случайный нож", cost: 1200 },
  { name: "Огненный змей", cost: 450 },
  { name: "Тайное CS2", cost: 180 },
  { name: "Армейское качество", cost: 25 },
  { name: "Фарм кейс x10", cost: 350 },
];
const SIMULATED_DROPS = [
  { name: "АК-47 | Ледяной уголь (FT)", val: 140 },
  { name: "AWP | Горячие грёзы (MW)", val: 190 },
  { name: "M4A4 | Зубной оскал (BS)", val: 45 },
  { name: "P250 | Песчаные дюны (FN)", val: 0.8 },
  { name: "USP-S | Сайрекс (MW)", val: 240 },
  { name: "Штык-нож | Градиент (FN)", val: 8900 },
  { name: "Перчатки | Мята (FT)", val: 6200 },
  { name: "Агент | Синяя метка", val: 80 },
];

export default function StreamFeed() {
  const [logs, setLogs] = useState<AuditLog[]>([]);
  const logIdCounter = useRef(0);

  useEffect(() => {
    const initialLogs: AuditLog[] = Array.from({ length: 6 }).map((_, i) => {
      logIdCounter.current += 1;
      const site = SIMULATED_SITES[Math.floor(Math.random() * SIMULATED_SITES.length)];
      const user = SIMULATED_USERS[Math.floor(Math.random() * SIMULATED_USERS.length)];
      const itemCase = SIMULATED_CASES[Math.floor(Math.random() * SIMULATED_CASES.length)];
      const drop = SIMULATED_DROPS[Math.floor(Math.random() * SIMULATED_DROPS.length)];
      const marketValue = drop.val > itemCase.cost * 1.5 && Math.random() > 0.8 
        ? drop.val 
        : Math.round(itemCase.cost * (0.05 + Math.random() * 0.45) * 10) / 10;

      return {
        id: `log-${logIdCounter.current}`,
        timestamp: new Date(Date.now() - i * 4000).toLocaleTimeString("ru-RU", { hour12: false }),
        user,
        site,
        caseName: itemCase.name,
        cost: itemCase.cost,
        dropName: drop.name,
        marketValue,
        isProfit: marketValue > itemCase.cost,
      };
    });
    setLogs(initialLogs);

    const interval = setInterval(() => {
      logIdCounter.current += 1;
      const site = SIMULATED_SITES[Math.floor(Math.random() * SIMULATED_SITES.length)];
      const user = SIMULATED_USERS[Math.floor(Math.random() * SIMULATED_USERS.length)];
      const itemCase = SIMULATED_CASES[Math.floor(Math.random() * SIMULATED_CASES.length)];
      
      const dropPool = itemCase.cost > 400 
        ? SIMULATED_DROPS 
        : SIMULATED_DROPS.filter(d => d.val < 500);
      const drop = dropPool[Math.floor(Math.random() * dropPool.length)];
      
      const isLucky = Math.random() > 0.94;
      const marketValue = isLucky 
        ? Math.round((itemCase.cost * (1.1 + Math.random() * 0.8)) * 10) / 10
        : Math.round((itemCase.cost * (0.05 + Math.random() * 0.35)) * 10) / 10;

      const newLog: AuditLog = {
        id: `log-${logIdCounter.current}`,
        timestamp: new Date().toLocaleTimeString("ru-RU", { hour12: false }),
        user,
        site,
        caseName: itemCase.name,
        cost: itemCase.cost,
        dropName: drop.name,
        marketValue,
        isProfit: marketValue > itemCase.cost,
      };

      setLogs((prev) => [newLog, ...prev.slice(0, 5)]);
    }, 2800);

    return () => clearInterval(interval);
  }, []);

  return (
    <div className="flex flex-col h-full bg-zinc-950/60 border border-zinc-800 p-3 rounded-2xl liquid-glass overflow-hidden">
      <div className="flex items-center justify-between border-b border-zinc-800 pb-2 mb-3">
        <div className="flex items-center gap-2">
          {/* Кастомный анимированный SVG-пульс телеметрии */}
          <svg className="w-4.5 h-4.5 text-rose-500 animate-pulse" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
            <path d="M22 12h-4l-3 9L9 3l-3 9H2" />
          </svg>
          <span className="text-[11px] font-mono tracking-wider text-zinc-400">ТЕЛЕМЕТРИЯ АУДИТА (LIVE)</span>
        </div>
        <div className="flex items-center gap-1.5 px-2 py-0.5 rounded bg-zinc-900 border border-zinc-800">
          <div className="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-ping" />
          <span className="text-[10px] font-mono text-emerald-400">ONLINE</span>
        </div>
      </div>

      <div className="flex-1 flex flex-col gap-1.5 overflow-hidden">
        <AnimatePresence initial={false} mode="popLayout">
            {logs.map((log) => (
            <motion.div
              key={log.id}
              layoutId={log.id}
              initial={{ opacity: 0, y: -20, scale: 0.95 }}
              animate={{ opacity: 1, y: 0, scale: 1 }}
              exit={{ opacity: 0, scale: 0.9, y: 15 }}
              transition={{ type: "spring", stiffness: 140, damping: 15 }}
              className={`flex flex-col gap-1 p-2 rounded border transition-all duration-300 ${
                log.isProfit 
                  ? "border-emerald-500/30 bg-emerald-500/5 glow-emerald" 
                  : "border-zinc-800 bg-zinc-900/40 hover:border-zinc-700"
              }`}
            >
              <div className="flex items-center justify-between text-[10px] font-mono text-zinc-500">
                <div className="flex items-center gap-1.5">
                  <Terminal className="w-3.5 h-3.5 text-zinc-400" />
                  <span className="text-zinc-300 font-bold truncate max-w-20">{log.user}</span>
                  <span>→</span>
                  <span className="text-zinc-400 underline decoration-zinc-700">{log.site}</span>
                </div>
                <span>{log.timestamp}</span>
              </div>

              <div className="flex items-center justify-between text-xs font-mono">
                <div className="flex flex-col">
                  <span className="text-zinc-400 text-[10px]">Кейс:</span>
                  <span className="text-zinc-100 font-semibold">{log.caseName}</span>
                </div>
                <div className="text-right">
                  <span className="text-zinc-400 text-[10px]">Цена:</span>
                  <span className="text-rose-400 block font-bold">{log.cost} ₽</span>
                </div>
              </div>

              <div className="flex items-center justify-between mt-1 border-t border-zinc-800 pt-1 text-xs font-mono">
                <div className="flex flex-col">
                  <span className="text-zinc-500 text-[9px]">Выпало:</span>
                  <span className="text-zinc-300 truncate max-w-37.5 font-medium">{log.dropName}</span>
                </div>
                <div className="text-right flex items-center gap-1">
                  {log.isProfit ? (
                    <ArrowUpRight className="w-3.5 h-3.5 text-emerald-400" />
                  ) : (
                    <ArrowDownRight className="w-3.5 h-3.5 text-rose-500" />
                  )}
                  <span className={`font-bold ${log.isProfit ? "text-emerald-400" : "text-zinc-400"}`}>
                    {log.marketValue} ₽
                  </span>
                </div>
              </div>
              
              <div className="flex justify-between items-center text-[9px] font-mono mt-0.5 text-zinc-500">
                <span>RTP сессии:</span>
                <span className={log.isProfit ? "text-emerald-400" : "text-rose-400"}>
                  {Math.round((log.marketValue / log.cost) * 100)}%
                </span>
              </div>
            </motion.div>
          ))}
        </AnimatePresence>
      </div>

      <div className="mt-2 text-[9px] font-mono text-zinc-500 flex items-center gap-1 justify-center border-t border-zinc-800 pt-2">
        <Shield className="w-3 h-3 text-emerald-500" />
        <span>Данные шифруются согласно регламенту защиты</span>
      </div>
    </div>
  );
}

